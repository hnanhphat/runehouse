import React from "react";

const MainVisual = ({ heading }) => {
  return (
    <div id="main-visual" className="main-visual">
      <h1>{heading}</h1>
    </div>
  );
};

export default MainVisual;
